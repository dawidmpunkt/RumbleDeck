  {
  "name": "RumbleDeck",
  "version": 0.0.2",
  "description": "A plugin to setup the DRV2605-based rumble mod via I2C. It also sniffs USB communcation for Rumble signals and sends triggers via I2C.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
+   "postinstall": "make -C backend || echo '*** Skipping backend build (no toolchain). Build manually: make -C backend'"
  },
  "dependencies": {
-   "decky-frontend-lib": "^4.x"
+   "@decky/ui": "^4.9.2"
  },
  "devDependencies": {
-   "rollup": "^3.x",
-   "typescript": "^5.2"
+   "rollup": "^4.0.0",
+   "typescript": "^5.4.0",
+   "@rollup/plugin-node-resolve": "^15.2.3",
+   "@rollup/plugin-commonjs": "^25.0.7",
+   "@rollup/plugin-typescript": "^11.1.6"
  }
}
